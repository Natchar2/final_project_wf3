<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="row col-xs-12">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button>
				<a class="navbar-brand logo" href="{{ url('accueil') }}">SC</a>
			</div>

			<div id="navbar" class="navbar-collapse collapse">
				<ul class="nav navbar-nav">
					<li><a href="{{ url('accueil') }}">Home</a></li>
					<li><a href="{{ url('shop') }}">Shop</a></li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Blog<span class="caret"></span></a>
						<ul class="dropdown-menu">
							<li><a href="{{ url('accueil_forum') }}">Forum</a></li>
							<li><a href="{{ url('agenda') }}">Agenda</a></li>
						</ul>
					</li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-search fa-3" aria-hidden="true" style="color:white;"></i><span class="caret"></span></a>
						<ul class="dropdown-menu">
							<form action="{{ url('search') }}" class="navbar-form navbar-left" method="post">
								
									<input type="text" name="searchString" class="form-control" required/>
									<button type="submit" class="btn btn-block" style="margin-top:7px;"><i class="fa fa-search fa-3" aria-hidden="true" style="color:black;"></i></button>
								
							</form>
						</ul>
					</li>
				</ul>

				

				<ul class="nav navbar-nav navbar-right">
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<li><a href="{{ path('deconnexion') }}">Déconnexion</a></li>
						<li><a href="{{ url('suivi') }}">Suivi commandes</a></li>
					{% else %}
						<li><a href="{{ url('inscription_post') }}">Inscription</a></li>
						<li><a href="{{ url('connexion') }}">Connexion</a></li>
					{% endif %}

					<li><a href="{{ url('panier') }}">Panier (<span class="total_product">{{ app.session.get('total_product') }}</span>)</a></li>
					
					
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<li class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
							{% if app.user.getAvatar() is not empty %}
                            	<img src="{{ asset('assets/images/avatar/'~app.user.getAvatar()) }}" style="width:auto;height:30px;">
							{% endif %}
                            <span class="caret"></span></a>
							<ul class="dropdown-menu">
								<li><a href="{{ url('profil', {'ID_user': app.user.ID_user}) }}">Mon profil</a></li>
								<li><a href="{{ url('listProducts') }}">Mes articles</a></li>
								<li><a href="{{ url('list_events') }}">Mes évènements</a></li>
								<li><a href="{{ url('list_topics') }}">Mes topics</a></li>
								{% if app.user.type[0] == 'ROLE_ADMIN' %}
								<li><a href="{{ url('accueilAdmin') }}">Mon administration</a></li>
							{% endif %}
							</ul>
						</li>
                    {% endif %}
				</ul>
			</div>
			<!--/.nav-collapse -->
		</div>
	</div>
</nav>
<div class="space-container">
